import React from 'react'

function EmptyMessage({message}) {
  return (
    <div className='fixed top-[50%] right-[50%] text-3xl  font-bold from-neutral-900'>{message}</div>

  )
}

export default EmptyMessage
